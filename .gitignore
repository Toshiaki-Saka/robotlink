# ── Build directories ────────────────────────────────────────────────
build/
out/
cmake-build-*/
_deps/

# ── Out-of-scope for this release (kept locally only) ────────────────
# Unintegrated GUI frontends (not built by CMake, not in CI). Kept locally;
# may be split into their own repo later. The published scope is the
# symbolic-derivation → C++ codegen → sim → matplotlib visualiser pipeline.
frontend_avalonia/
frontend_qt/
# Redundant visualiser variants — the supported one is python/visualizer.py.
python/visualizer_matplotlib.py
python/visualizer_pyqt6.py
python/visualizer_pyside.py
# Scratch debug images
good.png
問題.png

# ── Generated code (produced at build time by derive_and_export.py) ───
generated/
*.generated.hpp

# ── Simulation output ────────────────────────────────────────────────
output/
*.csv

# ── Generated media (regenerate locally; keep repo small) ────────────
# NOTE: a small demo GIF for the README may be committed under docs/
*.gif
*.png
*.mp4
!docs/*.gif
!docs/*.png

# ── CMake ────────────────────────────────────────────────────────────
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
install_manifest.txt
compile_commands.json

# ── MSVC / Visual Studio ─────────────────────────────────────────────
*.vcxproj
*.vcxproj.filters
*.vcxproj.user
*.sln
*.suo
*.user
.vs/
x64/
Debug/
Release/
*.obj
*.exe
*.pdb
*.ilk

# ── Python ───────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
env/
.pytest_cache/
*.egg-info/

# ── .NET / Avalonia ──────────────────────────────────────────────────
[Bb]in/
[Oo]bj/
*.user
*.userosscache
*.nupkg
*.nuget.props
*.nuget.targets
project.nuget.cache
project.assets.json

# ── Editors / OS ─────────────────────────────────────────────────────
.claude/
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
